自定义包  结构

(ushort)包长度 = 包头        固定占用2个字节    
(bool)包体是否压缩           固定占用1个字节
(ushort)CRC16校验            固定占用2个字节  这里是为了验证是否丢包
剩下的包 xor异或算法         剩下的字节都是真包  这里是为了防止有人解包
经过反异或解析               得到真正的数据包

数据包 =   数据包的前2个字节(ushort)协议编码 + (剩下的数据包)协议内容